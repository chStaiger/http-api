
# The name of the REST class
class: PIDEndpoint
# The python file where your REST class is located
# (inside the 'restapi/confs/custom' dir)
file: extended

mapping:
  all: "/pids"
  single: "/pids/<string:pid>"
labels:
  - eudat
  - pids
